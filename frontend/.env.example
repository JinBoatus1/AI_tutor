# ---------- 浏览器里请求的 API 根地址（会打进构建产物）----------
# 生产构建默认读仓库里的 .env.production（已指向 Render 时可不改）。
# 若前后端同域反代 /api，可删 .env.production 里的 VITE_API_URL 或留空以走相对路径。
# 部署在 Vercel 时：vercel.json 已把 /api 代理到 Render；此时 VITE_API_URL 可留空。若换了后端域名，请同步改 vercel.json 里的 destination。
# VITE_API_URL=https://your-api.example.com

# ---------- 仅本机 npm run dev / vite preview：Vite 把 /api 代理到哪里 ----------
# 默认与 .env.production 相同（公网 Render）。若 API_BASE 为空且只连本机 uvicorn，可设为下面一行（端口按你本机为准）。
# DEV_API_PROXY_TARGET=http://127.0.0.1:8000
